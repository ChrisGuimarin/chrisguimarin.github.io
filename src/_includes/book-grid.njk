<div class="book-grid">
{%- for book in books -%}
    <figure>
        <a href="{{ book.data.link }}" target="_blank" rel="noopener noreferrer">
            <img src="{{ book.data.cover }}" alt="{{ book.data.title }} book cover">
            <figcaption>
                <strong>{{ book.data.title }}</strong><br>
                {{ book.data.author }}
            </figcaption>
        </a>
    </figure>
{%- endfor -%}
</div>